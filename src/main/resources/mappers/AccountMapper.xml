<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.almond.account.dao.AccountMapper">
 	
    <select id="selectAccountById" parameterType="String" resultType="account">
    	/* 사용자 조회 by ID */
    	SELECT idx,
    	       id,
    	       password,
    	       loginKey,
    	       regDate
    	FROM memopad_account
    	WHERE id = #{id}
    </select>
 	
    <select id="selectAccountByKey" parameterType="String" resultType="account">
    	/* 사용자 조회 by Key */
    	SELECT idx,
    	       id,
    	       password,
    	       loginKey,
    	       regDate
    	FROM memopad_account
    	WHERE loginKey = #{key}
    </select>
 	
    <select id="signin" parameterType="account" resultType="account">
    	/* 로그인 */
    	SELECT idx,
    	       id,
    	       password,
    	       loginKey,
    	       regDate
    	FROM memopad_account
    	WHERE id = #{id}
    </select>
 
    <insert id="signup" parameterType="account">
    	/* 회원가입 */
    	INSERT INTO memopad_account (
   	        id,
   	        password,
   	        regDate
    	)
    	VALUES (
    		#{id},
    		#{password},
    		NOW()
    	)
    </insert>
    
    <update id="updateKey" parameterType="account">
    	/* 로그인 key 갱신 */
    	UPDATE memopad_account 
    	   SET loginKey = #{key}
    	 WHERE id = #{id}
    </update>
 
</mapper>