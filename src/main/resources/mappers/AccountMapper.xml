<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.almond.account.dao.AccountMapper">
 	
    <select id="signin" parameterType="account" resultType="account">
    	/* 로그인 */
    	SELECT idx,
    	       id,
    	       password,
    	       regDate
    	FROM memopad_account
    	WHERE id = #{id}
    </select>
 
</mapper>